@model IEnumerable<CoverType>
@{
    ViewData["Title"] = "Index";
}




<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Cover Type List</h2>
        </div>
        <div class="col-6">
           <a  asp-controller="CoverType" asp-action="Create" class="btn btn-primary p-2"><i class="bi bi-plus-circle"></i>&nbsp; Create New Cover Type</a>
        </div>
    </div>
    <br />
   
    <table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>Cover Type Name</th>
             <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in @Model)
        {
            <tr>
                <td width="50%">@item.name</td>
                  <td>
                      <div class="w-75 btn-group" role="group">
                          <a class="btn btn-primary actions me-4" asp-controller="CoverType" asp-action="Edit" asp-route-id="@item.id">
                              <i class="bi bi-pencil-square"></i>
                          </a>
                          <a class="btn btn-danger actions" asp-controller="CoverType" asp-action="Delete" asp-route-id="@item.id">
                             <i class="bi bi-trash3-fill"></i>
                          </a>
                      </div>
                     
         
                  </td>
            </tr>
        }
    </tbody>
</table>

</div>
